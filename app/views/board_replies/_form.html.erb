<%= form_for(board_reply, url: url) do |f| %>
  <% if board_reply.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(board_reply.errors.count, "error") %> prohibited this board_reply from being saved:</h2>

      <ul>
      <% board_reply.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, class: "form-control" %>
  </div>

  <br>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content, class: "form-control" %>
  </div>

  <br>

  <div class="actions">
    <%= f.submit class: "btn btn-outline-primary" %>
  </div>
<% end %>
